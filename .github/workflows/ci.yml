name: CI

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - uses: actions/setup-node@v7
        with:
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - run: npm test

  worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v6.0.2

      - uses: actions/setup-node@v7
        with:
          cache: 'npm'
          cache-dependency-path: worker/package-lock.json

      - run: npm ci

      # No test suite; dry-run bundles src/index.ts without Cloudflare creds.
      - run: npx wrangler deploy --dry-run
